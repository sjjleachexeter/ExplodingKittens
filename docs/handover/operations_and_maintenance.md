# update deployment
1. update repository `git pull`
2. rebuild docker `docker compose up --build`


# Manger Roles
1. create superuser account
2. signin to superuser on the website
3. use role admin form to assign roles to individual users

# Backups
Backups can be made by copping the `/DjangoFramework/db.sqlite3` into a secure location